import { PageHeader } from '@/components/layout/PageHeader';

// Placeholder while the screen is being built.
export default function AppointmentPage() {
  return <PageHeader title="Appointment" />;
}
